import React from 'react';
import Routes from './routes';
import './assets/styles/index.scss';

interface AppProps {}

function App({}: AppProps) {
  /** I just create a simple routing component */
  return <Routes />;
}

export default App;
